$('#like-icon-post-<%= @board.id.to_s %>').
  html('<%= link_to "いいねを取り消す", board_likes_path(@board), method: :POST, remote: true, class: "love hide-text" %>');
$('#like-text-post-<%= @board.id.to_s %>').
  html('<%= j render "boards/like_text", { likes: @board.likes } %>');